const avg_ratio = require('./avg_ratio');
const largest_ratio = require('./lagest_ratio');
const long_inflow = require('./long_inflow');
const rising_inflow_ratio = require('./rising_inflow_ratio');
const rising_inflow = require('./rising_inflow');
const largest = require('./largest');

module.exports = {
    avg_ratio,
    largest_ratio,
    long_inflow,
    rising_inflow_ratio,
    rising_inflow,
    largest,
}
